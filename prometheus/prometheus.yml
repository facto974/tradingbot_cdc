global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: cryptocom_trader
    static_configs:
      - targets:
          - host.docker.internal:8000   # exporter de l'agent (host)
          - agent:8000                  # si lancé via docker-compose --profile full
